meta {
  name: Logout
  type: http
  seq: 5
}

post {
  url: {{SERVICE_URL}}/api/v1/auth/logout
  body: none
  auth: inherit
}

script:post-response {
  if (res.status === 200) {
     bru.setVar("USER_AUTH_ACCESS_TOKEN", "");
     bru.setVar("USER_AUTH_REFRESH_TOKEN", "");
  }
}
